# ============================================================
# 数据库配置示例文件
# ============================================================
# 使用说明:
# 1. 复制此文件为 .env (不要提交 .env 到 git)
# 2. 根据实际环境修改配置值
# 3. 多环境支持: 可创建 .env.dev, .env.test, .env.prod
# ============================================================

# 环境选择 (可选)
# 如果设置了 ENV 变量,会优先加载 .env.{ENV} 文件
# 示例: ENV=dev 会加载 .env.dev
ENV=dev

# ============================================================
# 数据库连接配置 (必需)
# ============================================================

# 数据库主机地址
DB_HOST=localhost

# 数据库端口 (默认: 5432)
DB_PORT=5432

# 数据库名称
DB_NAME=qinqiang02

# 数据库用户名
DB_USER=qinqiang02

# 数据库密码 (如果没有密码,留空)
DB_PASSWORD=

# ============================================================
# 表名配置 (可选)
# ============================================================

# 表名后缀 (包含下划线)
# 示例: _1201 表示金税四期 2024年12月01日批次
# 如果不设置或留空,表名将不带后缀
# 示例:
#   设置 TABLE_SUFFIX=_1201 → 表名为 t_sim_original_bill_1201
#   设置 TABLE_SUFFIX=       → 表名为 t_sim_original_bill
TABLE_SUFFIX=_1201

# ============================================================
# 业务逻辑配置 (可选)
# ============================================================

# 整行红冲判定阈值 (单位: 元)
# 当蓝票行剩余可红冲金额 ≤ 此阈值时,视为整行红冲
# 默认值: 0.1 (即剩余金额在 [-0.01, 0.10] 元之间视为整行红冲)
# 注意: 由于浮点数计算精度问题,可能出现微小负数(-0.01),也会被视为整行红冲
FULL_ROW_THRESHOLD=0.1

# ============================================================
# 多环境配置示例
# ============================================================
#
# 开发环境 (.env.dev):
#   DB_HOST=localhost
#   DB_NAME=qinqiang02_dev
#   TABLE_SUFFIX=_1201
#
# 测试环境 (.env.test):
#   DB_HOST=test-db.example.com
#   DB_NAME=qinqiang02_test
#   TABLE_SUFFIX=_1201
#
# 生产环境 (.env.prod):
#   DB_HOST=prod-db.example.com
#   DB_NAME=qinqiang02_prod
#   TABLE_SUFFIX=_1201
#
# ============================================================
